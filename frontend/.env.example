# Base URL of the backend API (see ../Backend). Defaults to this value
# in src/lib/api.ts even if unset, so this file is optional for local
# dev against a backend running on the default port -- only needed if
# your backend runs elsewhere.
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
